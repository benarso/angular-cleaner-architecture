import { ObservableUseCase } from './observable-use-case';

describe('ObservableUseCase', () => {
  it('should create an instance', () => {
    expect(new ObservableUseCase()).toBeTruthy();
  });
});
